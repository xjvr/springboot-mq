server:
  port: 6666

spring:
  application:
    name:
  activemq:
    broker-url: tcp://110.40.136.42:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: true   # false = Queue, true = Topic

# 队列名称
myqueue: springboot-mq-queue

# 主题名称
mytopic: springboot-mq-topic